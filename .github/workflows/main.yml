name: App CD

on:
  pull_request:
    branches: ["main"]

  push:
    branches: ["main"]

jobs:
  main:
    runs-on: macos-latest
    steps:    
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: "17.x"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.29.3'

      - name: Get Flutter packages
        run: |
          flutter pub get

      - name: Build APK
        run: flutter build apk --release
        
      - name: Install Firebase CLI
        run: |
          curl -sL https://firebase.tools | bash

      - name: Upload to Firebase App Distribution(Android)
        run: |
          firebase appdistribution:distribute build/app/outputs/flutter-apk/app-release.apk \
            --app "${{ secrets.APP_ANDROID_ID }}" \
            --groups "testers" \
            --release-notes "Nova build ANDROID automática via GitHub Actions" \
            --token "${{ secrets.FIREBASE_TOKEN }}"

      # - name: Build iOS App
      #   run: |
      #     flutter build ios --release --no-codesign
      #     cd build/ios/iphoneos
      #     mkdir Payload
      #     cd Payload
      #     ln -s ../Runner.app
      #     cd ..
      #     zip -r app.ipa Payload

      # - name: Upload to Firebase App Distribution(iOS)
      #   run: |
      #     firebase appdistribution:distribute build/ios/iphoneos/app.ipa \
      #       --app "${{ secrets.APP_IOS_ID }}" \
      #       --groups "testers" \
      #       --release-notes "Nova build automática iOS via GitHub Actions" \
      #       --token "${{ secrets.FIREBASE_TOKEN }}"  


      # - name: Upload Artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: Releases
      #     path: |
      #       build/app/outputs/flutter-apk/app-release.apk
      #       build/app/outputs/bundle/release/app-release.aab
      #       build/ios/iphoneos/app.ipa



